version: '3.9'

services:
  bun-latest:
    container_name: 'bun-latest'
    build:
      context: ../../
      dockerfile: ./test/docker/bun/latest.Dockerfile
    environment:
      NODE_ENV: production

  bun-canary:
    container_name: 'bun-canary'
    build:
      context: ../../
      dockerfile: ./test/docker/bun/canary.Dockerfile
    environment:
      NODE_ENV: production

  deno-latest:
    container_name: 'deno-latest'
    build:
      context: ../../
      dockerfile: ./test/docker/deno/latest.Dockerfile
    environment:
      NODE_ENV: production

  node-08:
    container_name: 'node-08'
    build:
      context: ../../
      dockerfile: ./test/docker/node/08.Dockerfile
    environment:
      NODE_ENV: production

  node-10:
    container_name: 'node-10'
    build:
      context: ../../
      dockerfile: ./test/docker/node/10.Dockerfile
    environment:
      NODE_ENV: production

  node-12:
    container_name: 'node-12'
    build:
      context: ../../
      dockerfile: ./test/docker/node/12.Dockerfile
    environment:
      NODE_ENV: production

  node-14:
    container_name: 'node-14'
    build:
      context: ../../
      dockerfile: ./test/docker/node/14.Dockerfile
    environment:
      NODE_ENV: production

  node-16:
    container_name: 'node-16'
    build:
      context: ../../
      dockerfile: ./test/docker/node/16.Dockerfile
    environment:
      NODE_ENV: production

  node-18:
    container_name: 'node-18'
    build:
      context: ../../
      dockerfile: ./test/docker/node/18.Dockerfile
    environment:
      NODE_ENV: production

  node-20:
    container_name: 'node-20'
    build:
      context: ../../
      dockerfile: ./test/docker/node/20.Dockerfile
    environment:
      NODE_ENV: production

  node-latest:
    container_name: 'node-latest'
    build:
      context: ../../
      dockerfile: ./test/docker/node/latest.Dockerfile
    environment:
      NODE_ENV: production
