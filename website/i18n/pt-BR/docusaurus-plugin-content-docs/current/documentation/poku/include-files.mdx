---
sidebar_position: 1
---

import { History } from '@site/src/components/History';
import { Stability } from '@site/src/components/Stability';

# 📦 Incluir Diretórios e Arquivos

Por padrão, o **Poku** procura os arquivos _`.test.`_ e `.spec.` , mas você pode customizá-lo usando a opção [`filter`](/docs/documentation/poku/options/filter).

<History
  records={[
    {
      version: '2.1.0',
      changes: [
        <>Suporte a múltiplos caminhos em qualquer ordem.</>,
        <>
          Flag <code>--include</code> depreciada.
        </>,
        <>
          Mantém suporte retroativo para múltiplos caminhos separados por vírgulas para evitar breaking changes.
        </>,
      ],
    },
  ]}
/>

## CLI

### Uso comum

```bash
# Same as ./
npx poku
```

- Executa todos os testes sequencialmente.

```bash
# Same as ./
npx poku --parallel
```

- Executa todos os testes em paralelo.

:::tip
Você pode passar tanto diretórios quanto arquivos.
:::

:::note
Não é possível executar testes nos diretórios `.git` e `node_modules`.
:::

<hr />

### Definindo os diretórios como o último argumento

```bash
npx poku targetPath
```

```bash
npx poku targetPathA targetPathB
```

<hr />

### `--include`

<Stability
  level={0}
  message={
    "Agora é possível passar múltiplos caminhos em qualquer posição desde a v2.1.0"
  }
/>

<hr />

## API

> `poku(targetPaths: string | string[])`

```ts
await poku('targetPath');
```

```ts
await poku(['targetPathA', 'targetPathB']);
```

```ts
await poku('./');
```

<blockquote>

Em seguida, execute o arquivo diretamente com a plataforma de sua escolha, por exemplo:

```bash
node test/run.test.js
```

```bash
npx tsx test/run.test.ts
```

</blockquote>
