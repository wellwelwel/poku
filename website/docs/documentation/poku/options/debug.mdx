---
sidebar_position: 6
---

# `debug`

By default **Poku** doesn't shows logs that doesn't comes from **Poku**'s **`assert`**, but you can enable them:

## CLI

```bash
npx poku --debug ./test
```

## API

```ts
poku('./test', {
  debug: true,
});
```
