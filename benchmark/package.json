{
  "type": "module",
  "scripts": {
    "start": "sh benchmark.sh | tee output.md && node comparison.js",
    "bench:execution": "sh benchmark.sh execution | tee output.md && node comparison.js execution",
    "bench:assertions": "sh benchmark.sh assertions | tee output.md && node comparison.js assertions",
    "bench:nesting": "sh benchmark.sh nesting | tee output.md && node comparison.js nesting",
    "bench:general": "sh benchmark.sh general | tee output.md && node comparison.js general"
  },
  "devDependencies": {
    "chai": "^6.2.2",
    "jest": "30.3.0",
    "mocha": "11.7.5",
    "poku": "file:..",
    "vitest": "4.1.2"
  }
}
